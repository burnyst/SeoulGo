<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="reply">
	<!-- 댓글 작성 -->
	<insert id="replyProc" parameterType="com.seoulmate.seoulgo.dto.NoticeReplyDTO">
		insert into noticeReply
		values ((select NVL(MAX(nrNo),0)+1 from noticeReply), 1,
		(select memberID from member_t where memberID=#{memberID}), #{nrContent}, SYSDATE);
	</insert>
</mapper>